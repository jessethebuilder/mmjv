<style>
  body{
    padding-top: 50px;
  }
</style>

<div id='top_nav' class='navbar navbar-fixed-top navbar-default navbar-inverse' role='navigation'>
  <div class="container">
    <div class='navbar-header'>
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#top_nav_body">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Medical Marijuana Vendors", root_path, :class => 'navbar-brand' %>
    </div>

    <div id='top_nav_body' class='collapse navbar-collapse'>
      <ul class='nav navbar-nav navbar-right'>
        <!--<li>-->
          <%#= link_to 'Products', products_path %>
        <!--</li>-->

        <%# if user_is_admin? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Products <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'All', products_path %></li>
              <% ProductType.all.each do |pt| %>
                <%= content_tag :li do %>
                  <%= link_to pt.name, product_type_path(pt)  %>
                <% end %>
              <% end %>
            </ul>
          </li>
        <%# end %>

        </li>

        <% if user_signed_in? %>
          <li>
            <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
          </li>
        <% else %>
          <li>
            <%= link_to 'Login', new_user_session_path %>
          </li>
          <!--<li style="font-weight:800;">-->
          <!--<%#= link_to 'Sign Up', new_user_registration_path %>-->
          <!--</li>-->
        <% end %>
        </li>
      </ul>
    </div>
  </div>
</div>